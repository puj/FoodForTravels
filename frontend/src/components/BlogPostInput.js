import React from 'react'

import { EditText, EditTextarea } from 'react-edit-text'
import 'react-edit-text/dist/index.css'

export const BlogPostInput = ({ titlevalue, textvalue, titleChange, textChange }) => {
  return (
    {/* <>
      <EditText placeholder='Add title' value={titlevalue} onChange={titleChange}/>
      <EditTextarea placeholder='Add text' value={textvalue} onChange={textChange}/>
    </> */}
  )
}
